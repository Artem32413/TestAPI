basePath: /api/v1
definitions:
  structs.Analytics:
    properties:
      productId:
        description: Идентификатор товара
        example: PRD-1001
        type: string
      soldgoods:
        description: Количество проданных товаров
        example: 50
        type: integer
      totalsum:
        description: Общая сумма продаж
        example: 12500.5
        type: number
      warehouseId:
        description: Идентификатор склада
        example: WH-001
        type: string
    type: object
  structs.AnalyticsSwagger:
    properties:
      warehouseId:
        description: Идентификатор склада
        example: 9be99fa0-2cd1-4c4e-863d-5115095fbd09
        type: string
    type: object
  structs.TopAnalytics:
    description: Структура данных топовых складов по выручке
    properties:
      addr:
        description: Адрес склада
        example: ул. Ленина, 10
        type: string
      totalsum:
        description: Общая выручка склада
        example: 150000.75
        type: number
      warehouseId:
        description: Идентификатор склада
        example: WH-001
        type: string
    type: object
host: localhost:8080
info:
  contact:
    email: support@example.com
    name: API Support
    url: http://www.example.com/support
  description: This is a sample API Go server with Zap logging
  license:
    name: Apache 2.0
    url: http://www.apache.org/licenses/LICENSE-2.0.html
  title: API Go Service
  version: "1.0"
paths:
  /analytics/:
    post:
      consumes:
      - application/json
      description: Возвращает аналитику продаж по указанным параметрам
      parameters:
      - description: Параметры запроса аналитики
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/structs.AnalyticsSwagger'
      produces:
      - application/json
      responses:
        "200":
          description: Успешный ответ
          schema:
            items:
              $ref: '#/definitions/structs.Analytics'
            type: array
        "400":
          description: Ошибка в запросе
        "500":
          description: Внутренняя ошибка сервера
      summary: Получить аналитику продаж
      tags:
      - Analytics
  /analytics/top/:
    get:
      consumes:
      - application/json
      description: Возвращает список складов с наибольшей выручкой
      produces:
      - application/json
      responses:
        "200":
          description: Список топовых складов
          schema:
            items:
              $ref: '#/definitions/structs.TopAnalytics'
            type: array
        "400":
          description: Ошибка в запросе
        "500":
          description: Внутренняя ошибка сервера
      summary: Получить топ складов по выручке
      tags:
      - Analytics
schemes:
- http
swagger: "2.0"
